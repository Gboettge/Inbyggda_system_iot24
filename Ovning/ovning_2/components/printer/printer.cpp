#include <stdio.h>
#include "printer.h"

namespace ovning_2
{
    int func()
    {
        return 2;
    }
    int printNumbers(int start, int stop)
    {
        while (start != stop){
            //printer
            start ++;
        }
        return 0;
    }
}
